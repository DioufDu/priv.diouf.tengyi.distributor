(function() {
	// Controller
	angular.module('canteen.app').controller('canteen.contact.controller', [
	// Dependencies
	'$scope', '$amui',
	// Main
	function($scope, $amui) {
		$amui.load();
	} ]);
})()
